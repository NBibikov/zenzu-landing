---
interface Props { t: any }
const { t } = Astro.props

const steps = [
  { step: '01', ...t.howItWorks.step1 },
  { step: '02', ...t.howItWorks.step2 },
  { step: '03', ...t.howItWorks.step3 },
]
---

<section id="how-it-works" class="py-24 px-6 bg-paper-2">
  <div class="max-w-3xl mx-auto">
    <div class="mb-16">
      <h2 class="font-serif text-3xl md:text-4xl text-ink mb-4">{t.howItWorks.title}</h2>
      <p class="text-ink-2 text-lg">{t.howItWorks.subtitle}</p>
    </div>

    <div>
      {steps.map(({ step, title, desc }) => (
        <div class="flex items-baseline gap-8 py-8 border-t border-hairline">
          <span class="font-serif text-2xl text-ink-3 tabular-nums shrink-0">{step}</span>
          <div>
            <h3 class="font-serif text-xl text-ink mb-2">{title}</h3>
            <p class="text-ink-2 leading-relaxed">{desc}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
