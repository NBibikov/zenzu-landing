---
import '../styles/global.css'

interface Props {
  title: string
  description?: string
  ogImage?: string
  lang?: string
  ogType?: 'website' | 'article'
  /** Used for og:title / twitter:title — defaults to `title` without any " — Zenzu" suffix noise. */
  ogTitle?: string
  /** hreflang alternates: { hreflang, href }. An x-default entry is auto-added from the en/default one. */
  alternates?: { hreflang: string; href: string }[]
  /** JSON-LD structured data object (rendered as application/ld+json). */
  jsonLd?: Record<string, unknown>
  /** Keep the page out of the index (404 etc.): emits robots noindex and drops the canonical. */
  noindex?: boolean
}

const {
  title,
  description = 'Learn languages from real content — YouTube, podcasts, articles. AI flashcards with spaced repetition.',
  ogImage = '/og-default.png',
  lang = 'en',
  ogType = 'website',
  ogTitle,
  alternates,
  jsonLd,
  noindex = false,
} = Astro.props

const canonicalURL = new URL(Astro.url.pathname, Astro.site)
const ogImageURL = new URL(ogImage, Astro.site)
const socialTitle = ogTitle ?? title
---

<!doctype html>
<html lang={lang} class="antialiased">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />

    <!-- SEO -->
    <title>{title}</title>
    <meta name="description" content={description} />
    {noindex
      ? <meta name="robots" content="noindex" />
      : <link rel="canonical" href={canonicalURL} />}

    <!-- hreflang alternates -->
    {alternates?.map(alt => (
      <link rel="alternate" hreflang={alt.hreflang} href={alt.href} />
    ))}
    {alternates && alternates.length > 0 && (
      <link
        rel="alternate"
        hreflang="x-default"
        href={(alternates.find(a => a.hreflang === 'en') ?? alternates[0]).href}
      />
    )}

    <!-- Open Graph -->
    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={socialTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageURL} />
    <meta property="og:site_name" content="Zenzu" />
    <meta property="og:locale" content={lang} />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={socialTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageURL} />

    {jsonLd && (
      <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
    )}

    <link rel="alternate" type="application/rss+xml" title="Zenzu Blog" href="/rss.xml" />

    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/zenzu-icon.png" />
    <link rel="apple-touch-icon" href="/zenzu-icon.png" />

    <!-- Fonts: Lora (content) + Golos Text (UI). Non-render-blocking. -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..600;1,400..500&family=Golos+Text:wght@400..600&display=swap"
      media="print"
      onload="this.media='all'"
    />
    <noscript>
      <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400..600;1,400..500&family=Golos+Text:wght@400..600&display=swap" />
    </noscript>
  </head>
  <body class="bg-paper text-ink font-sans antialiased">
    <slot />
  </body>
</html>
